،workflows:
  android-workflow:
    name: Android APK
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: |
          flutter pub get
          flutter build apk --release
          find . -name "*.apk" -exec echo "Found: {}" \;
    artifacts:
      - "**/*.apk"
