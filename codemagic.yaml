workflows:
  android-workflow:
    name: Android AAB
    environment:
      flutter: stable
    scripts:
      - name: Build AAB
        script: |
          flutter pub get
          flutter build appbundle --release
    artifacts:
  - build/**/*.aab
